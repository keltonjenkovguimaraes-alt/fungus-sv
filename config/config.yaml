# FUNGUS-SV Configuration
# ========================

# Sample names (match FASTQ files in data/raw/)
samples:
  - test_sample

# Reference genome
reference:
  fasta: "data/reference/reference.fasta"
  gff: "data/reference/reference.gff"

# ICB Consensus Parameters (from Liu et al. 2024)
icb:
  callers:
    - pbsv
    - sniffles2
    - cutesv
  min_callers: 2
  min_overlap: 0.5
  flank: 200

# Validation layer weights (from Liu et al. 2024 Nature Comms)
# LAR > Depth = k-mer > Breakpoint > Ploidy
weights:
  local_assembly: 0.30
  depth_signature: 0.25
  kmer_spectrum: 0.25
  breakpoint_junction: 0.20
  ploidy_confirmation: 0.0  # hard filter, not scored

# T-score thresholds (UNcalibrated — spike-in pending)
tscore_thresholds:
  high_confidence: 0.80
  medium_confidence: 0.60
  low_confidence: 0.40

# Minimum SV size
min_sv_size: 50

# Ploidy
ploidy: haploid
max_het_rate: 0.07  # 7% max heterozygous SNVs for haploid fungi
